const notes = ['Note1', 'Note2', 'Note3']

notes.forEach(function(item, index)
{
    console.log(` Position no. ${index} has value  ${item} `)
})

for(let i=0; i<notes.length; i++)
{
    console.log(notes[i])
}

